# Todo

- [ ] Set up an actual newsletter system — create the account (e.g. Buttondown) and configure the real backend (set `VITE_BUTTONDOWN_USERNAME`), so the newsletter form actually subscribes instead of faking success