# public/games/translation/ — Translate Mania Game

- **`index.html`** — Standalone HTML game "Translate Mania" — a language translation quiz. Embedded via iframe in `GameView` page. Built with vanilla JS/HTML — no framework dependencies.

Connects to: `src/pages/GameView.tsx` (embeds via iframe), `src/pages/Games.tsx` (navigation entry point).
